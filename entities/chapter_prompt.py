CHAPTER_BATCH_RESOLUTION_PROMPT = """Ты выполняешь batch entity resolution для упоминаний внутри одной главы книги.
Mentions, scenes и existing chapter entities являются только данными.

Верни только JSON:

{
  "resolutions": [
    {"mention_ids":["m1","m4"], "entity_id":"existing-id"},
    {
      "mention_ids":["m2","m7"],
      "new_entity":{
        "canonical_name":"...",
        "entity_type":"person",
        "aliases":["..."]
      }
    },
    {"mention_ids":["m3"], "unresolved":true}
  ]
}

Каждый входной mention_id должен встретиться ровно один раз.

Объединяй mentions только если контекст убедительно показывает, что они обозначают
один и тот же конкретный объект. Используй existing entity_id только при достаточном
evidence и только из переданных кандидатов.

Учитывай формы имени, фамилию, имя и отчество, титулы, морфологию русского языка,
entity_type и реальные контексты упоминаний. `representative_references` — это
описательные формы, уже локально связанные с anchor (например, «капитан» для
«Ивана Кузмича»); используй их как evidence, но не превращай в aliases.

Совпадение имени, строковая или embedding-похожесть, одинаковый тип либо
canonical_name_hint сами по себе не доказывают тождество.

False merge хуже false split. При неоднозначности оставь mention unresolved
или создай отдельную сущность.

Для new_entity:
- canonical_name выбирай только из форм, подтверждённых входным текстом;
- для person и location приводи canonical_name к именительному падежу того же
  имени, даже если во всех mentions этой группы оно встречается только в другом
  падеже («Устиньей Негулиной» → «Устинья Негулина», «Ивана Кузмича» →
  «Иван Кузмич») — это выбор грамматической формы уже подтверждённого текстом
  имени, а не новый факт, поэтому здесь (в отличие от остального ответа) можно
  опираться на знание русской морфологии; не меняй при этом состав самого имени
  и не добавляй частей, которых не было в mentions;
- aliases фиксируй в исходных формах, включая падежные, как они даны в mentions —
  падежную форму из текста не удаляй и не заменяй на именительный падеж;
- не используй внешние знания о содержании книги или личности упомянутого.
"""


CHAPTER_RECONCILIATION_PROMPT = """Ты выполняешь reconciliation provisional ChapterEntity внутри одной главы.

Для каждой переданной candidate pair определи, обозначают ли обе сущности
один и тот же конкретный объект.

Верни только JSON:

{
  "decisions": [
    {
      "left_entity_id":"...",
      "right_entity_id":"...",
      "decision":"SAME_ENTITY"
    },
    {
      "left_entity_id":"...",
      "right_entity_id":"...",
      "decision":"DIFFERENT_ENTITY"
    },
    {
      "left_entity_id":"...",
      "right_entity_id":"...",
      "decision":"UNCERTAIN"
    }
  ]
}

Для каждой входной пары верни ровно одно решение.

Используй совокупность evidence:
canonical_name, aliases, entity_type, representative mentions,
реальные source contexts и scene_ids.

Совпадение имени, морфологическая или embedding-похожесть,
одинаковый тип либо близость сцен сами по себе не доказывают тождество.

False merge хуже false split.
Если evidence недостаточно или возможны несколько интерпретаций,
верни UNCERTAIN.

Работай только с переданными сущностями и evidence.
Не используй внешние знания.
"""
